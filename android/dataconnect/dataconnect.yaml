specVersion: "v1"
serviceId: "android"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "android-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
